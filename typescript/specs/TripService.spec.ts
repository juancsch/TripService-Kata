import TripService from '../src/trip/TripService'
import UserNotLoggedInException from '../src/exception/UserNotLoggedInException'

describe('TripService specifications', () => {

	test('should... ', () => {

		expect(2 + 2).toBe(5)
	})
})
